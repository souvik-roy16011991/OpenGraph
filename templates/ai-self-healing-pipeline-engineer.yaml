slug: ai-self-healing-pipeline-engineer
name: AI Self-Healing Pipeline Engineer
description: >
  Fixes CI/CD pipelines automatically — detects and resolves pipeline
  failures in real time (flaky tests, lockfile conflicts, missing
  secrets, stale caches) so developers aren't pager-duty for the build.
category: engineering
icon: RefreshCw
domain:
  domain_name: Self-Healing CI/CD
  domain_display_name: AI Self-Healing Pipeline Engineer
  organization_name: Developer Experience & Build Platform
  knowledge_focus_examples: |
    flaky-test taxonomies (timing, ordering, resource, async, test
    pollution), test-quarantine vs auto-retry strategy, dependency-
    resolution failure patterns (npm, pip, Maven, Go modules, Cargo),
    build-cache invalidation heuristics, secret / credential-expiry
    detection, Docker layer-cache behaviour, test-impact-analysis
    (TIA), merge-queue and trunk-based development principles
  tool_focus_examples: |
    CI / build platforms (GitHub Actions, GitLab CI, Jenkins, Buildkite,
    CircleCI, Drone, Azure Pipelines), flaky-test detectors (Launchable,
    TestChimp, Trunk.io Flaky Tests, DataDog CI Visibility), merge
    queues (Graphite Merge Queue, Mergify, Aviator), secret managers
    (HashiCorp Vault, Doppler, AWS Secrets Manager), build-cache engines
